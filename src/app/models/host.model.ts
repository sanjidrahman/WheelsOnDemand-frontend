export interface hostModel {
    _id: string
    name: string
    email: string
    phone: number
    password: string
    isBlocked: boolean
    isVerified: boolean
    document: string
    __v: number
}